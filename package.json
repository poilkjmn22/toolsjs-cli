{
  "name": "@toolsjs-cli/root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "jest",
    "build": "lerna run build",
    "prepare": "lerna run prepare",
    "bootstrap": "lerna bootstrap",
    "clean": "lerna clean",
    "version": "lerna version",
    "publish": "lerna publish"
  },
  "devDependencies": {
    "jest": "^29.6.0",
    "lerna": "^7.1.1",
    "ts-jest": "^29.1.1"
  },
  "volta": {
    "node": "22.14.0"
  }
}
